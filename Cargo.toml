[workspace]
resolver = "3"
members = ["crates/xmuhub-core", "crates/xmuhub-server"]

[workspace.package]
edition = "2024"
version = "0.1.0"
license = "MIT"

[profile.release]
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
opt-level = 3
